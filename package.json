{
  "name": "hero-command-center",
  "private": true,
  "scripts": {
    "build": "esbuild ./app.tsx --outfile=app.js --minify"
  },
  "dependencies": {
    "@netlify/blobs": "^10.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.27.7"
  }
}
